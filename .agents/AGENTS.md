# Git Commit & Push Rules
- ALWAYS ask for the user's explicit authorization before executing `git commit` and `git push`.
- Prepare all the necessary files, stage them if necessary, and then STOP. Wait for the user to confirm before running the commit and push commands.
